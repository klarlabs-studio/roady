{
  "name": "roady-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && rm -rf ../docs/*.html ../docs/_astro && cp -r dist/* ../docs/ && touch ../docs/.nojekyll",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/vue": "^6.0.1",
    "astro": "^6.4.8",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "rollup": "^4.59.0",
    "tailwindcss": "^4.0.0"
  }
}
