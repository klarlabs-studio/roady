---
// Commands Section.
//
// REWRITTEN for v0.11. Was: 7 groups x ~28 commands = the feature museum
// the README explicitly killed. Now: the same 5-step workflow that the
// README leads with, with one CTA to the full reference. Power users go
// to docs/advanced.md or `roady --help`.
---

<section id="commands" class="py-24">
  <div class="max-w-4xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">The five commands you'll actually use</h2>
      <p class="text-gray-400 max-w-2xl mx-auto">
        Roady has 31 top-level commands. You will spend 95% of your time in five of them.
        Here they are.
      </p>
    </div>

    <!-- The actual workflow code block -->
    <div class="glass rounded-3xl p-2 shadow-2xl overflow-hidden mb-8">
      <div class="bg-[#0B0E14] rounded-2xl p-6 mono text-xs md:text-sm leading-relaxed overflow-x-auto">
        <div class="flex space-x-2 mb-4">
          <div class="w-3 h-3 bg-red-500/50 rounded-full"></div>
          <div class="w-3 h-3 bg-yellow-500/50 rounded-full"></div>
          <div class="w-3 h-3 bg-green-500/50 rounded-full"></div>
        </div>
        <pre class="text-gray-300"><code><span class="text-gray-500"># 1. Hook your AI agent to Roady (one command per supported tool)</span>
<span class="text-violet-400">roady setup</span> claude-code     <span class="text-gray-500"># or claude-desktop, opencode, openai, gemini</span>

<span class="text-gray-500"># 2. Initialise + import your existing docs</span>
<span class="text-violet-400">roady init</span> my-project
<span class="text-violet-400">roady spec analyze</span> docs/    <span class="text-gray-500"># parses markdown, captures source citations</span>

<span class="text-gray-500"># 3. Generate a plan (deterministic; --ai emits a prompt for your model)</span>
<span class="text-violet-400">roady plan generate</span>
<span class="text-violet-400">roady plan approve</span>

<span class="text-gray-500"># 4. Drive execution from inside your AI editor</span>
/roady-task                  <span class="text-gray-500"># agent picks the next ready task</span>
<span class="text-gray-500"># ...agent implements, commits with [roady:task-id] marker...</span>
<span class="text-violet-400">roady git sync</span>              <span class="text-gray-500"># state moves forward automatically</span>

<span class="text-gray-500"># 5. Ask the question that matters</span>
<span class="text-violet-400">roady drift detect</span>          <span class="text-gray-500"># has reality diverged from intent?</span>

<span class="text-gray-500"># 6. Keep everyone else informed (since v0.13)</span>
<span class="text-violet-400">roady report</span>                <span class="text-gray-500"># progress doc: markdown, html, or json</span>
<span class="text-violet-400">roady notify digest</span>         <span class="text-gray-500"># push one summary to Slack</span>
<span class="text-violet-400">roady audit trail</span> task-42   <span class="text-gray-500"># which agent did this, and what proves it</span>

<span class="text-gray-500"># Bonus — multiple projects per repo (since v0.11)</span>
<span class="text-violet-400">roady</span> -P feature-auth <span class="text-violet-400">init</span> --template minimal
<span class="text-violet-400">roady</span> -P feature-auth <span class="text-violet-400">task ready</span></code></pre>
      </div>
    </div>

    <!-- Two callouts: demo + full reference -->
    <div class="grid md:grid-cols-2 gap-6">
      <div class="glass p-6 rounded-2xl">
        <h3 class="text-white font-semibold mb-2 flex items-center">
          <i data-lucide="play" class="w-4 h-4 text-emerald-400 mr-2"></i>
          See it before you commit to anything
        </h3>
        <p class="text-sm text-gray-400 mb-3">
          <code class="mono text-violet-400">roady demo</code> scaffolds a sample
          project with intentional drift and runs the full loop in under a minute.
          Zero AI keys, zero signup.
        </p>
        <code class="text-xs mono text-gray-500 bg-black/30 px-2 py-1 rounded block break-all">brew install felixgeelhaar/tap/roady &amp;&amp; roady demo</code>
      </div>
      <div class="glass p-6 rounded-2xl">
        <h3 class="text-white font-semibold mb-2 flex items-center">
          <i data-lucide="book-open" class="w-4 h-4 text-violet-400 mr-2"></i>
          Need the full reference?
        </h3>
        <p class="text-sm text-gray-400 mb-3">
          Billing, debt scoring, deps graph, watch mode, dashboards, plugins,
          audit verify &mdash; everything else lives in
          <code class="mono text-violet-400">docs/advanced.md</code>, grouped by
          audience.
        </p>
        <a
          href="https://github.com/felixgeelhaar/roady/blob/main/docs/advanced.md"
          class="inline-flex items-center text-violet-400 hover:text-violet-300 text-sm font-medium"
        >
          See all 31 commands
          <i data-lucide="arrow-right" class="w-3 h-3 ml-1"></i>
        </a>
      </div>
    </div>
  </div>
</section>
